import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.SunStuff.app',
  appName: 'SunStuff',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
